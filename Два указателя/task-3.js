/*
Дан целочисленный массив nums, переместить все 0 элементы в его конец,
сохраняя относительный порядок ненулевых элементов.

Обратите внимание, что это необходимо сделать на месте, не создавая копию массива.

Ввод: числа = [0, 1, 0, 3, 12]
Вывод: [1, 3, 12, 0, 0]
*/

const moveZeroes = function (nums) {
  let left = 0;

  for (let right = 0; right < nums.length; right++) {
    if (nums[right] !== 0) {
      [nums[right], nums[left]] = [nums[left], nums[right]];
      // деструктурирующее присваивание, меняет местами значения элементов массива)
      left++;
    }
  }

  return nums;
};

console.log(moveZeroes([0, 1, 0, 3, 12]));
// [1, 3, 12, 0, 0]

/*
Оба указателя (left и right) движутся слева направо:

right пробегает по всему массиву, проверяя каждый элемент.

left ждёт на месте и сдвигается только тогда, когда right находит ненулевое число. 
После обмена left делает шаг вправо.

Таким образом, left всегда указывает на позицию, 
куда должен быть помещён следующий ненулевой элемент, и никогда не обгоняет right. 
*/
